<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:MoneyMindApp.ViewModels"
             x:Class="MoneyMindApp.Views.BiometricSetupPage"
             x:DataType="vm:BiometricSetupViewModel"
             Title="Sicurezza">
    <VerticalStackLayout Spacing="20" Padding="20" VerticalOptions="Center">
        <Label Text="🔐" FontSize="80" HorizontalOptions="Center" />
        <Label Text="Proteggi i tuoi dati" FontSize="24" FontAttributes="Bold" HorizontalTextAlignment="Center" />
        <Label Text="Abilita Face ID/Touch ID per proteggere l'accesso all'app" HorizontalTextAlignment="Center" />
        <Button Text="Abilita Autenticazione Biometrica" Command="{Binding EnableBiometricCommand}" />
        <Button Text="Salta" Command="{Binding SkipCommand}" BackgroundColor="Transparent" TextColor="{StaticResource Primary}" />
    </VerticalStackLayout>
</ContentPage>
